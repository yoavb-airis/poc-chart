apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-module.fullname" . }}-test-connection"
  labels:
    {{- include "ai-module.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-module.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
